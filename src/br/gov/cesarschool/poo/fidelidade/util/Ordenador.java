package br.gov.cesarschool.poo.fidelidade.util;

import br.gov.cesarschool.poo.fidelidade.geral.entidade.Identificavel;

public class Ordenador {
	public static void ordenar(Identificavel[] ident) {
		for(int i = 0 ; i < ident.length ; i++) {
			for(int j = i+1 ; ident.length ; i++) {
				if(ident[i].comparar(ident[j]) < 0) {
					Identificavel temp = // continuar
				}
			}
		}
	}
}
